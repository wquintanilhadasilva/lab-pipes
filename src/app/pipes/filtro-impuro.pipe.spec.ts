import { FiltroImpuroPipe } from './filtro-impuro.pipe';

describe('FiltroImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
